syntax = "proto3";

package chat;

// Este fluxo é bidirecional e permite enviar e receber as mensagens
service ChatService {
  rpc Chat (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
  string username = 1;
  string message = 2;
  int64 timestamp = 3;
}